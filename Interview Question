Q1 What is a Transport?
Answer

A transport defines where logs are stored or sent. 
Common transports include the console, files, databases, and cloud logging services.

Q2 Why timestamps?
Answer

Timestamps help identify exactly when an event occurred, making debugging and monitoring much easier in production.

Q3 Why centralize logging?
Answer

Instead of scattering console.log() throughout the codebase, a centralized logger provides consistent formatting, log levels, and makes it easy to change logging behavior in one place.





Q1 Difference between INFO and ERROR?
Answer

INFO logs record successful application events, while ERROR logs capture failures and exceptions that require attention.

Q2 Why WARN instead of ERROR for Redis lock?
Answer

Failure to acquire a lock is an expected business scenario due to concurrent requests. It is not an application failure, so it should be logged as a warning rather than an error.

Q3 Why not log everything as ERROR?
Answer

If every event is logged as an error, real production issues become difficult to identify because logs become noisy.




Interview Questions
Q1 Why Global Error Handler?
Answer

It centralizes error handling, removes duplicate try-catch logic, ensures consistent API responses, and improves maintainability.

Q2 Why should error middleware be registered last?
Answer

Express executes middleware in order. The error handler must come after all routes so it can catch errors propagated from controllers.

Q3 Difference between throw error and next(error)?
Answer

throw creates the error, while next(error) passes it to Express's centralized error-handling middleware.



Interview Question
Q. Why create a custom ApiError instead of using Error?
Answer

The default Error object only stores the error message. A custom ApiError allows us to include HTTP status codes and other metadata, making error handling consistent across the application.





8. Interview Questions
Q1. Why not use JavaScript Error?
Answer

JavaScript's default Error only stores the error message. It doesn't include HTTP status codes or API-specific information. A custom ApiError allows us to return the correct status code and maintain consistent API responses.

Q2. Why extend Error?
Answer

By extending Error, we inherit all the default error behavior while adding our own properties like statusCode and success.

Q3. Why Error.captureStackTrace()?
Answer

It records the exact location where the error occurred, making debugging easier in production.


Interview Questions
Q1 Why validate request before controller?
Answer

To reject invalid input as early as possible, preventing unnecessary business logic execution and database queries.

Q2 Why create a separate validators folder?
Answer

It separates validation logic from business logic, making the code easier to maintain, test, and reuse.

Q3 Why not validate in service?
Answer

Services should focus on business logic. Input validation is an API-layer concern and should happen before the request reaches the service.

Why didn't you use the same validation middleware for GET APIs?

Answer

Because the current middleware validates only req.body. GET requests usually send data through req.query or req.params. We need a more flexible validation middleware that can validate body, query, and params separately.


7. Interview Questions
Q1

Why did you make one middleware for body, query and params?

Answer

I wanted a reusable validation middleware. Instead of creating three separate middlewares, I used a dynamic property (req[property]) so the same middleware can validate body, query, or params.

Q2

Why validate query parameters?

Answer

Query parameters are user input just like the request body. Invalid query values can cause incorrect database queries or application errors, so they should be validated before reaching the controller.

Q3

Why validate route params?

Answer

Route parameters often contain IDs. Validating them ensures the application receives the correct data type before executing business logic.




8. Interview Questions
Q1. Why use server.close() instead of process.exit() directly?

Answer:

server.close() stops accepting new requests but allows existing requests to complete. Calling process.exit() directly can terminate active requests abruptly and may lead to data inconsistency.

Q2. Why handle both SIGINT and SIGTERM?

Answer:

SIGINT is typically triggered by pressing Ctrl + C during development, while SIGTERM is commonly sent by Docker, Kubernetes, or process managers during graceful shutdown in production.

Q3. Why call pool.end()?

Answer:

It closes all PostgreSQL connections cleanly, preventing connection leaks and freeing database resources.

Q4. Why call redisClient.quit()?

Answer:

It gracefully closes the Redis connection and ensures pending commands are handled before disconnecting.


Interview Questions
Q1

Why Swagger?

Answer

Swagger provides interactive API documentation. Frontend developers and testers can understand and test APIs without reading backend code.

Q2

Why not write API documentation manually?

Answer

Manual documentation easily becomes outdated. Swagger keeps the documentation synchronized with the API implementation.




Interview Questions
Q1

Swagger kya hai?

Answer

Swagger is an OpenAPI-based tool used to generate interactive API documentation.

Q2

Swagger UI kya karta hai?

Answer

Swagger UI provides a browser interface where developers can read API documentation and test endpoints.

Q3

Swagger JsDoc kya karta hai?

Answer

It scans JSDoc comments from route files and converts them into an OpenAPI specification.



Interview Questions
Q1

Swagger comments routes me hi kyu likhte ho?

Answer

Because swagger-jsdoc scans route files directly and generates the OpenAPI specification automatically, keeping documentation close to the implementation.

Q2

Swagger aur Joi me same fields kyu?

Answer

Joi validates incoming requests at runtime, while Swagger documents the API contract. Keeping both aligned ensures the documentation accurately reflects the implemented API.

Q3

GET /trains me requestBody kyu nahi likha?

Answer

Because this endpoint does not accept a request body. It only returns data, so only response documentation is required.


Interview Questions
Q1

GET API me requestBody kyu nahi use ki?

Answer

GET requests generally use query parameters instead of request bodies. Swagger documents them using parameters with in: query.

Q2

Difference between requestBody and query parameter?

Answer

requestBody is sent in the HTTP body, typically with POST/PUT requests. Query parameters are appended to the URL and are commonly used with GET requests.

Q3

Why document query parameters?

Answer

It clearly communicates the required inputs to API consumers and enables interactive testing through Swagger UI.



Interview Questions
Q1

Why document requestBody?

Answer

It allows API consumers to understand the exact JSON payload required and enables interactive testing through Swagger UI.

Q2

Why should Swagger match Joi validation?

Answer

Swagger defines the API contract, while Joi enforces it. If they differ, the documentation becomes misleading and API consumers may send invalid requests.

Q3

Why include error responses like 404 and 409?

Answer

Clients need to know all possible outcomes of an API call so they can handle different scenarios correctly, such as validation failures, missing resources, or business conflicts.



Interview Questions
Q1

Difference between

in:path

and

in:query
Answer

in: path documents route parameters (e.g. /users/{id}), while in: query documents URL query parameters (e.g. /search?name=abc).

Q2

Why add BearerAuth in Swagger?

Answer

It enables authenticated API testing directly from Swagger UI without manually adding the Authorization header for every request.

Q3

Why document security in Swagger?

Answer

It clearly communicates which endpoints require authentication and improves developer experience for frontend and QA teams.




Q1. Difference between defining securitySchemes and using security?

Answer:

securitySchemes defines how authentication works (JWT Bearer in our case).
security on an endpoint specifies which endpoints require that authentication.
Q2. Why not add JWT security to every API?

Answer:

Some APIs are intentionally public, such as searching trains or viewing available seats. Only operations that require an authenticated user should enforce JWT authentication.

Q3. Why is Swagger Authorize useful?

Answer:

It allows developers and QA engineers to test protected APIs directly from Swagger UI without manually adding the Authorization header for every request.





15. Interview Questions
Q1

Difference between Unit Testing and Integration Testing?

Answer

Unit Testing verifies an individual function in isolation by mocking its dependencies.

Integration Testing verifies that multiple components (such as routes, controllers, services, and the database layer) work correctly together.

Q2

Why use Jest?

Answer

Jest provides assertions, mocking, snapshots, test runners, and coverage reporting in a single framework, making it the standard testing framework for Node.js applications.

Q3

Why install Jest as a devDependency?

Answer

Because Jest is only required during development and testing. It is not needed when running the application in production.


Interview Questions
Q1

Why did we mock pool.connect() instead of pool.query()?

Answer

Because the bookTicket() service creates a database client using pool.connect() and performs all transaction queries (BEGIN, SELECT, INSERT, UPDATE, COMMIT) through client.query(). Mocking pool.query() would not intercept those calls.

Q2

Why is emit created with jest.fn()?

Answer

jest.fn() creates a mock function that records how it was called. This allows us to verify later that the service emitted the expected Socket.IO event.




Interview Questions
Q1
Why use mockResolvedValueOnce() instead of mockResolvedValue()?

Answer

Because client.query() is called multiple times with different purposes during a transaction. Each call needs a different mocked response to accurately simulate the execution flow.

Q2
Why did we return { rows: [{ status: "AVAILABLE" }] }?

Answer

The service checks:

const seat = seatResult.rows[0];

if (seat.status !== "AVAILABLE")

So the mock must match the exact structure returned by PostgreSQL.

Q3
Why is the order of mocks important?

Answer

Jest consumes mockResolvedValueOnce() values sequentially. If the order does not match the actual database query sequence, the service receives incorrect data and the test fails.



🎤 Interview Questions
Q1
Why verify client.query() call count?

Answer:

It ensures the service executed the expected database transaction flow (BEGIN, SELECT, INSERT, UPDATE, COMMIT). If a query is skipped or an extra query is added unintentionally, the test will fail.

Q2
Why verify releaseLock()?

Answer:

Locks must always be released, even if an error occurs. Verifying releaseLock() ensures the cleanup logic in the finally block is executed correctly.

Q3
Why verify emit()?

Answer:

Booking success should notify connected clients through Socket.IO. The test confirms that the service triggers this side effect.




🎤 Interview Question
Agar interviewer puche:

"Tumne emit ko .not.toHaveBeenCalled() kyu likha?"

Answer:

"Because the booking operation failed before completion. Socket events should only be emitted after a successful booking. Verifying that no event is emitted prevents incorrect side effects."\\\


Interview Question
Agar interviewer bole

"User validation fail hone ke baad DB connect kyu nahi hua?"

Answer:

Because the service follows the fail-fast principle. It validates the user before performing any database transaction, avoiding unnecessary resource usage.